pasos git

1) git init
2) git status #que rama, que commits
3) git add <archivo>
4) git remote add origin git@github.com...#le dice a git donde subir
                                          #los cambios
5) git commit -m "primer commit"#tiene los cambios hechos a los
                                #archivos debe tener comentarios claros
6) git push -u origin master #envía los cambios a donde los mandes
                             #-u origin es necesario solo la primera
                             #vez 
7) git clone <url>     #Clona el repositorio de la url
8) git pull            #Actualiza el repositorio desde la nube
9) git branch "nombre" #crea rama con nombre
10)git switch nombre   #se cambia a la rama nombre


